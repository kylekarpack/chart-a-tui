# chart-a-tui

This is a [Ratatui](https://ratatui.rs) app for displaying basic charts in the terminal.

Rust is a prerequisite. Once installed, clone this repo, navigate to the directory, and run:

```bash
cargo run
```

You can make incredibly simple charts by referencing a CSV file!
